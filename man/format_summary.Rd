% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_summary.R
\name{format_summary}
\alias{format_summary}
\title{Render a model summary as a knitr::kable table}
\usage{
format_summary(smry, format = "latex", digits = 3, ...)
}
\arguments{
\item{smry}{A \code{summary} object as returned by \code{summary()} for a
fitted \code{lm}, \code{glm}, \code{lme4::lmer}, \code{lme4::glmer}, \code{lmerTest::lmer},
\code{mgcv::gam}, or \code{mgcv::bam} model.}

\item{format}{A character of length 1. One of \code{"latex"} (default),
\code{"markdown"}, or \code{"rst"}. Passed through to \link[knitr:kable]{knitr::kable}.}

\item{digits}{Integer. Number of decimal places to show for numeric
cells. Default \code{3}.}

\item{...}{Method-specific arguments. The \code{summary.gam} method
accepts \code{gam_layout}: \code{"stacked"} (default) returns a single
table with embedded section-header rows \verb{(A. Parametric)} and
\verb{(B. Smooth)}; \code{"split"} returns a named list with elements
\code{parametric} and \code{smooth}, each a separate kable.}
}
\value{
For non-GAM summaries: a \code{knitr_kable} object. For GAM/BAM
summaries with \code{gam_layout="stacked"}: a \code{knitr_kable} object
(with a manually-assembled body for LaTeX). For GAM/BAM with
\code{gam_layout="split"}: a named \code{list(parametric, smooth)} of two
\code{knitr_kable} objects.
}
\description{
Takes a \code{summary()} object from a fitted model and returns a
\link[knitr:kable]{knitr::kable} suitable for inclusion in a LaTeX, Markdown, or
reStructuredText document. The function is an S3 generic that
dispatches on the class of the summary object. Methods are
provided for:
}
\details{
\itemize{
\item \code{lm}, \code{glm} (Gaussian or non-Gaussian families), \code{lme4::lmer},
\code{lme4::glmer}, and \code{lmerTest::lmer}: a single coefficient table
with columns Estimate, SE, optional df (only with \code{lmerTest}),
\emph{t} or \emph{z}, and optional \emph{p} (\code{lme4::lmer} produces none).
\item \code{mgcv::gam} and \code{mgcv::bam}: two sub-tables (parametric and
smooth). \code{gam_layout} controls whether they are combined into a
single stacked table with embedded section-header rows, or
returned as a named list of two separate kables.
}

p-values below 0.001 are formatted as \code{"<0.001"}. Column headers for
test statistics are wrapped in math italics: for LaTeX, \verb{$t$}, \verb{$z$},
\verb{$F$}, \verb{$\\chi^2$}, \verb{$p$}; for Markdown / RST, \verb{*t*}, \verb{*z*}, \verb{*F*},
the Unicode chi-squared character (U+03C7 U+00B2) wrapped in \verb{*...*},
and \verb{*p*}.
}
\examples{
\donttest{
set.seed(1); n <- 200
dat <- data.frame(y=rnorm(n), x1=rnorm(n), x2=rnorm(n))
fit <- lm(y ~ x1 + x2, data=dat)
format_summary(summary(fit), format='markdown')

library(mgcv)
g <- gam(y ~ x1 + s(x2), data=dat)
format_summary(summary(g), format='latex', gam_layout='stacked')
k <- format_summary(summary(g), format='latex', gam_layout='split')
k$parametric
k$smooth
}
}
\author{
Motoki Saito, \email{motoki.saito@uni-oldenburg.de}
}
\keyword{utilities}
