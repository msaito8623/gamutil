% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndat_to_contour.R
\name{ndat_to_contour}
\alias{ndat_to_contour}
\title{Contour plot from a data.frame with combinations of predictor values.}
\usage{
ndat_to_contour(
  ndat,
  x,
  y,
  z,
  z.lwr = "lwr",
  z.upr = "upr",
  break.interval = NULL,
  line.breaks = NULL,
  color.breaks = NULL,
  zlim = NULL
)
}
\arguments{
\item{ndat}{A data.frame with combinations of predictor values and predicted
values and confidence intervals.}

\item{x}{A character string with length 1 for the name of the variable used
for the x-axis.}

\item{y}{A character string with length 1 for the name of the variable used
for the y-axis.}

\item{z}{A character string with length 1 for the name of the variable used
for the z-axis (color).}

\item{z.lwr}{A character string with length 1 for the name of the column for
lower confidence interval boundary.}

\item{z.upr}{A character string with length 1 for the name of the column for
upper confidence interval boundary.}

\item{break.interval}{A numeric, indicating the interval from one to another
contour line. This argument is not used if "line.breaks" and
"color.breaks" are provided.}

\item{line.breaks}{A numeric vector for contour lines.}

\item{color.breaks}{A numeric vector for colors for the z-axis.}

\item{zlim}{A numeric vector with length 2, which indicates the range of the
z-axis.}
}
\value{
A ggplot object of a contour plot with predicted values being colors
(z-axis).
}
\description{
This function takes a new data.frame which contains combinations of
predictor values of interest (i.e., newdata or ndat) and returns a contour
plot from the data.frame. "ndat" necessary for this function can be
generated with gamutil::mdl_to_ndat and gamutil::add_fit.
}
\examples{
library(mgcv)
set.seed(534)
dat <- gamSim(verbose=FALSE)
model <- gam(y ~ s(x0) + s(x1) + s(x2) + ti(x0,x1), data=dat)
to_vary <- c('x0','x1')
ndat <- mdl_to_ndat(mdl=model, target=to_vary, len=10, method=median)
ndat <- add_fit(ndat, model)
plt <- ndat_to_contour(ndat, x='x0', y='x1', z='fit',
                       z.lwr='lwr', z.upr='upr')
print(plt)
}
\author{
Motoki Saito, \email{motoki.saito@uni-tuebingen.de}
}
\keyword{utilities}
